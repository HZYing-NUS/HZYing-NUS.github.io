# 个人网站使用说明

这份文档专门给梓颖看，告诉你怎么改文字、怎么加博客、怎么让网站上线。**不需要懂代码**，照着做就行。

---

## 一、文件夹长什么样

打开 `ziying-site` 文件夹，你会看到这些东西：

```
ziying-site/
├── index.html          主页（一般不用动）
├── 博客.html           博客文章模板（一般不用动）
├── 内容配置.js         你最常改的文件 ← 改主页文字看这里
├── 博客索引.js         加博客时改这个
├── 博客/               博客 .md 文章丢这里
├── 图片/               头像、二维码、各类证明图片
│   ├── 头像.jpeg
│   ├── 微信二维码.png
│   ├── 公众号二维码.png
│   ├── 教育/           毕业证、学位证、交换项目证书
│   ├── 项目/           项目结题、立项截图
│   ├── 论文/           论文发表截图
│   ├── 经历/           实习证明、雇佣证明
│   └── 奖项/           各类奖项、认证、荣誉的截图
└── 说明.md             就是你正在看的这份
```

**你只会经常碰这 4 处：`内容配置.js`、`博客索引.js`、`博客/`、`图片/`。**

---

## 二、改主页文字（中英双语）

1. 用任何文本编辑器打开 `内容配置.js`（VS Code、Sublime、甚至 macOS 自带的「文本编辑」都行；**不要**用 Word）。
2. 你会看到这些字段：`名字`、`一句话标签`、`自我介绍`、`教育列表`、`作品列表`、`论文列表`、`经历列表`、`奖项列表`、`邮箱`、`GitHub`、`微信号文字`、`公众号文字` 等等。
3. **只改双引号 `"..."` 里的文字，或反引号 ``` `...` ``` 里的文字。**其它符号（逗号、花括号 `{}`、方括号 `[]`）不要动。

### 双语字段怎么写

凡是「访客能看到的文字」字段都长这样：

```js
名字: {
  zh: "黄梓颖",        // ← 中文版
  en: "Ziying Huang",  // ← 英文版
},
```

只想要中文，**把 `en:` 那一行整行删掉就行**，切到英文时会自动回退显示中文：

```js
名字: {
  zh: "黄梓颖",
},
```

> URL / 邮箱 / 图片路径不分语言，一份就够：`邮箱: "ziying@example.com"`，不用写双语对象。

### 加一个新作品 / 经历 / 论文 / 教育

打开 `内容配置.js`，找到对应的列表（`作品列表`、`经历列表`、`论文列表`、`教育列表`），复制最上面的那一段（从 `{` 到 `},`），粘贴到列表里，再改字段就行。例子：

```js
作品列表: [
  // 原来的项目
  {
    时间: { zh: "2024 至今", en: "2024 — Present" },
    标题: { zh: "项目一", en: "Project One" },
    角色: { zh: "项目负责人", en: "Project Lead" },
    描述: { zh: "……", en: "..." },
    链接: "https://example.com",
    证明图片: ["图片/项目/项目一-结题.png"],
  },
  // 新加的项目（直接复制上面一段，改文字）
  {
    时间: { zh: "2026", en: "2026" },
    标题: { zh: "项目三", en: "Project Three" },
    角色: { zh: "独立开发", en: "Solo" },
    描述: { zh: "新项目的描述。", en: "New project description." },
    链接: "",
    证明图片: [],
  },
],
```

> **没有外链怎么办**：把 `链接: "..."` 写成 `链接: "",`（空引号）即可。
> **暂时不写英文**：把 `en: "..."` 那一行整行删掉，留下 `zh:` 即可。

### 「证明图片」字段怎么用

`教育列表`、`作品列表`、`论文列表`、`经历列表`、`奖项列表` 里都有 `证明图片` 字段，是一个数组：

```js
证明图片: ["图片/奖项/某某奖-证书.png"],
```

- 配了图片，前端就会在条目右下角出现「查看证书」按钮，点击弹窗放大看
- 多张图会自动支持左右翻页：`证明图片: ["图片/奖项/A.png", "图片/奖项/B.png"]`
- 没图片就写空数组 `[]`，按钮就不显示
- 把图片塞进 `图片/教育/`、`图片/项目/`、`图片/论文/`、`图片/经历/`、`图片/奖项/` 对应文件夹，文件名和这里写的一致即可

### 改头像 / 二维码

1. 把新的图片文件丢进 `图片/` 文件夹（建议 jpg / png，文件名用中文或英文都行）。
2. 打开 `内容配置.js`，把对应字段改成新的文件名：

   ```js
   头像图片: "图片/我的新头像.jpg",
   微信二维码图片: "图片/我的新二维码.png",
   公众号二维码图片: "图片/我的新公众号二维码.png",
   ```

### 改邮箱 / GitHub / 微信号 / 公众号名

```js
邮箱: "huangziying622@gmail.com",
GitHub: "https://github.com/HZYing-NUS",
微信号文字: { zh: "TheodoreGniy", en: "TheodoreGniy" },
公众号文字: { zh: "模方", en: "模方" },
```

GitHub 字段留空字符串 `""` 就不显示该板块。

---

## 三、加一篇新博客

发我 .md 文件我帮你加最省事，你自己来也行，三步：

1. **把 .md 文件丢进 `博客/` 文件夹**。建议命名格式：`年份-标题.md`，比如 `2026-我的第一篇.md`。
2. **打开 `博客索引.js`，复制最上面那一条**（从 `{` 到 `},`），粘贴到最顶上，改成新文章的信息：

   ```js
   {
     文件名: "2026-我的第一篇",       // 不要带 .md，要和文件名完全一致
     语言: "zh",                       // 中文博客填 "zh"，英文博客填 "en"
     标题: "我的第一篇",
     日期: "2026-05-22",
     摘要: "用一句话告诉读者这篇讲什么。",
     外链: "",                          // 一般留空；如果是发在外站的转载，填那个外站网址
   },
   ```

3. 保存。

> **关于「语言」字段**：决定切换到中 / 英时这篇是否显示。中文博客填 `"zh"`，英文博客填 `"en"`。访客切换到中文只看到 zh 的，切换到英文只看到 en 的。
> **关于「外链」字段**：如果文章你已经发在公众号 / 知乎 / 即刻了，不想搬到自己网站，那就把那个网址填到 `外链` 字段，访客点击会直接跳过去。如果留空，就跳到本站读你 .md 文件里的内容。
> **博客标题 / 摘要不需要写双语对象**：因为每篇博客本来就是一种语言写一份，所以这里就是普通字符串。

---

## 四、本地预览

**重要：不要双击 `index.html` 直接打开！** 这样打开博客子页会显示「加载失败」，因为浏览器对 `file://` 协议有限制。

推荐两种本地预览方式：

### 方式 1：VS Code + Live Server 插件（最省心）

1. 装 [VS Code](https://code.visualstudio.com/)。
2. 在 VS Code 左侧扩展商店搜「Live Server」，安装。
3. 用 VS Code 打开 `ziying-site` 文件夹，右键 `index.html` → 选「Open with Live Server」。
4. 浏览器会自动打开预览地址（一般是 `http://127.0.0.1:5500`）。

### 方式 2：直接推到 GitHub Pages 看（见下一节）

每次改完直接 push，1 分钟后线上就更新了。

---

## 五、第一次部署到 GitHub Pages

只需要做一次。以后每次改完只要「commit + push」就行。

> **注意**：GitHub Pages 免费版要求仓库公开，所以 `内容配置.js`、博客 .md、图片等所有源文件，**任何人都能在 GitHub 上浏览**。如果有不想公开的草稿或私人内容，丢进根目录的 `草稿/` 文件夹（已经在 `.gitignore` 里被忽略了，不会上传）。

### 准备工作

1. 注册一个 [GitHub](https://github.com) 账号。
2. 装 [GitHub Desktop](https://desktop.github.com/)（图形化工具，不用敲命令）。

### 创建仓库

1. 浏览器打开 https://github.com，登录后点右上角「+」→「New repository」。
2. **仓库名严格填写**：`你的GitHub用户名.github.io`
   - 比如你用户名是 `huangziying`，那仓库名就是 `huangziying.github.io`
   - **这个命名是关键**，能自动启用 GitHub Pages，不用手动配置
3. 选「Public」（公开）。
4. 「Initialize with README」**不勾选**。
5. 点「Create repository」。

### 把网站推上去

1. 打开 GitHub Desktop，点「File」→「Add local repository」→ 选你的 `ziying-site` 文件夹。
2. 它会提示「这不是一个 git 仓库」，点「create a repository」。
3. 在弹出窗口里：
   - Name 填 `你的GitHub用户名.github.io`（和上面创建的仓库名一致）
   - 其它默认，点「Create repository」
4. 顶部点「Publish repository」按钮，把 Public 勾选确认（不要勾「Keep this code private」），点「Publish Repository」。
5. 等 1 到 2 分钟，浏览器打开 `https://你的GitHub用户名.github.io`，应该就看到你的网站了。

### 之后每次改完怎么发布

1. 改完 `内容配置.js` 或 `博客索引.js` 或加完博客 .md。
2. 打开 GitHub Desktop，左下角写一句中文 commit message（比如「加了一篇新博客」），点「Commit to main」。
3. 顶部点「Push origin」。
4. 等 1 到 2 分钟，刷新 `https://你的GitHub用户名.github.io` 看更新。

---

## 六、打印为 PDF 简历

1. 主页右上角有一个「打印为 PDF」按钮，点击。
2. 在弹出的打印对话框里：
   - **打印机** 选「另存为 PDF」（Save as PDF）
   - **页面方向** 选「纵向」
   - **边距** 选「默认」
   - **背景图形** 不用勾（保持简洁）
3. 点「保存」。
4. PDF 里会自动**不包含**：博客正文、顶部导航栏、「打印」按钮本身、「查看证书」按钮、滚动浮标、灯箱。
5. PDF 里**会包含**：头像、自我介绍（所有章节自动展开）、教育、经历 / 作品、论文、奖项、博客标题 + 摘要、邮箱、GitHub、微信二维码、公众号二维码。

> 如果觉得 PDF 排版不理想，可以在打印预览里调「缩放」（比如改成 90%），或直接告诉我，我来调样式。

---

## 七、暗色模式

主页和博客子页都支持暗色模式。

- 第一次访问会跟随系统设置（macOS / iOS 在「外观」里切换深浅）
- 顶部导航有「日 / 月」按钮，点一下手动切换
- 你的选择会被记住，下次打开还是这个主题

---

## 八、常见问题

**Q：我改完文件，主页没变化？**
A：浏览器有缓存。按 `Cmd + Shift + R`（Mac）或 `Ctrl + F5`（Windows）强制刷新。

**Q：博客子页显示「加载失败」？**
A：99% 是因为你双击 `博客.html` 直接打开了。要么用 Live Server 预览，要么 push 到 GitHub Pages 后再访问。

**Q：我不小心删了某个符号（比如逗号），整个页面都白了？**
A：浏览器按 F12 打开「开发者工具」→「Console」标签，里面会用红字告诉你具体哪一行错了。如果看不懂，把错误信息发我，我帮你修。

**Q：「查看证书」按钮没出现？**
A：检查 `证明图片` 字段是不是空数组 `[]`，路径有没有写错（注意大小写、中英文字符），文件是不是真的在 `图片/` 对应子文件夹里。

**Q：我想加新的板块（比如「演讲」、「读书笔记」）？**
A：告诉我你想加什么，我来加。这超出了「改字段」的范围，需要动 `index.html`。

**Q：可以换个网址吗（不用 `xxx.github.io`）？**
A：可以买一个自己的域名（比如 `ziying.com`），然后做一些 DNS 配置。这块你不用学，等想换的时候告诉我，我一步步教。

---

## 九、需要我帮忙的时候

直接告诉我：

- 「帮我把这篇 .md 加到博客里」+ 文件
- 「帮我改一下自我介绍：……」
- 「加一个新项目：标题 X，时间 Y，描述 Z，链接 https://...」
- 「打印的 PDF 不好看，能不能让经历部分紧凑一点」

我来动文件，你只负责告诉我要什么。
