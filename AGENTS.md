# WebTools 项目协作指南

## 一、协作者

- 项目负责人是黄梓颖，称呼为「梓颖」。
- 梓颖是男生，使用「他」，不得因名字产生女性化称谓。
- 梓颖是创业者，不负责调试代码。交付必须完整、可运行、可验证。
- 沟通使用中文，简洁直接；中文内容使用中文标点，不使用 emoji。
- 不确定的信息明确标注「未验证」或「需确认」，发现问题必须直接指出。

## 二、项目是什么

项目名称为 WebTools，当前线上地址为：

- 当前产品：[https://webtools-flame.vercel.app/](https://webtools-flame.vercel.app/)；
- 最初个人网站：[https://hzying-nus.github.io/](https://hzying-nus.github.io/)；

WebTools 不是通用 AI 导航站。它主要服务已经会使用 AI 编程工具、但尚未独立跑通完整 Web 产品流程的初级用户。

核心问题是：从产品想法、原型、开发、数据库、部署，到 SEO、分析、客服和海外社区运营，用户需要使用大量分散的工具和资料，难以记忆，也不知道当前阶段该做什么。

WebTools 将这些资源按建站流程组织起来，并通过 AI 帮助用户找到下一步需要的工具、内容和方法。

## 三、当前产品结构

公开内容无需登录：

- 首页；
- 资源库；
- 行动专题；
- 博客；
- 关于我；
- 搜索。

互动功能需要登录：

- AI 对话；
- 项目和记忆；
- 文件上传；
- 投稿与修改建议；
- Credit 充值和记录。

未登录、已登录有余额、已登录余额不足仍是同一个产品，只改变权限、提示和操作结果，不开发三套界面。

## 四、AI 助手 V1

AI 助手定位文案只用于 AI 工作区，不替换网站整体定位：

> 把你的第一个 Web 产品做出来。

底层逻辑：

```text
大模型
＋WebTools 站内知识库
＋可选联网搜索
＋可选 Skill
＋项目记忆
＋文件上下文
→ 回答用户问题
```

关键规则：

- 站内知识库按问题自动检索，不提供关闭开关；
- 联网搜索和 Skill 由用户主动启用；
- 系统可以推荐 Skill，但必须由用户确认；
- Skill 推荐确认后创建新对话，并携带原问题和项目背景；
- 一条对话最多启用一个 Skill，不能直接切换为另一个 Skill；
- 普通用户只看到模型名称，不看到 Provider、中转站地址或 API Key；
- Provider 与用户可见模型解耦，后台可以切换实际渠道；
- 故障切换必须得到用户同意，不能静默切换。

V1 模型：

- `claude-haiku-4-5-20251001`；
- `claude-sonnet-4-6`；
- `claude-sonnet-5`；
- `claude-opus-4-6`；
- `claude-opus-4-8`；
- `deepseek-v4-flash`；
- `deepseek-v4-pro`。

模型和价格必须后台配置，不得硬编码在前端。具体价格与待确认项以 `docs/specs/webtools-ai-assistant-v1-spec.md` 为准。

## 五、项目与记忆

- 普通聊天可以不属于项目；
- 用户可以创建项目，并在项目内创建多条对话；
- 项目内记忆自动提炼，无需每次询问用户；
- 新项目对话应结合历史进度，提示已经完成什么和下一步建议；
- 项目外的全局记忆只保存长期、重复出现的重要信息；
- 项目外识别出的全局记忆先保存为待确认候选，用户确认前不得进入模型上下文；
- 用户可以查看、编辑和删除全局记忆；
- 删除对话时同步删除或重算由该对话产生的项目记忆；
- 删除项目进入 30 天回收站，到期后永久删除。

## 六、首个 Skill

首个 Skill 对外名称为「产品想法诊断」，中性化草案位于：

`docs/skills/product-idea-diagnosis/`

产品代码、文案、文件和输出中不得出现原始人物姓名、人物模仿、人物履历、个人口头禅或暗示官方授权的表达。

保留的方法论包括：

- 痛、钱、分发；
- 五类明确结论；
- 最危险假设；
- MVP 降级；
- B2B 决策链；
- 出海、定价、寻找 idea、竞品拆解和窗口期专题。

原始只读资料位于 `/Users/huangziying/.claude/skills/ask-liuxiaopai`。可以查看或复制后改写，绝不能修改原路径内容，也不要将未经中性化的原始副本提交到项目。

## 七、Credit 与支付

- V1 不做订阅，只做 Credit 一次性充值；
- 新用户赠送 10 Credit，30 天后过期；
- 购买 Credit 暂定长期有效；
- 支付渠道为 Creem；
- 所有 AI 请求必须先预扣，再调用模型；
- 流式生成支持滚动续扣；
- 余额不足时停止当前请求并保留已有回答；
- 充值后通过新请求继续回答，并按新增消耗收费；
- 平台故障产生的恢复费用由平台承担；
- 目标综合毛利率为 65%；
- 成本倍率为 2.857143；
- 1 Credit 的零售计价价值为 $0.05；
- 用户侧显示整数 Credit，内部账本保存高精度成本；
- 同一次用户发送的模型、搜索、文件和记忆成本先聚合，再统一向上取整；
- 最低消费为 1 Credit。

V1 充值包：

| 套餐   | Credit | 美元价格 |
| ------ | -----: | -------: |
| 入门包 |    300 |   $19.99 |
| 常用包 |    800 |   $44.99 |
| 进阶包 |  1,200 |   $64.99 |

常用包作为默认推荐档。套餐售价包含 Creem 手续费和运营缓冲，因此套餐的单 Credit 实付价格不要求等于 AI 请求结算使用的 `$0.05` 零售计价值。

邀请奖励计划：

- 有效邀请归因期为 30 天，每名新用户只能归属一个邀请人；
- 被邀请人注册并完成首次成功结算的 AI 请求后，邀请人获得 10 Credit；
- 每位邀请人每个自然月最多获得 100 Credit 注册奖励；
- 被邀请人首次充值后，邀请人获得基础套餐 Credit 的 10%，三档分别返还 30、80 和 120 Credit；
- 后续充值、节日赠送、人工赠送不参与邀请返还；
- 奖励经过 24 小时待确认和基础风控后到账；
- 退款、拒付、欺诈或作弊时撤回或冻结对应奖励；
- 所有邀请归因和奖励发放必须使用稳定幂等键。

节日优惠不在当前价格页或邀请奖励页面提前公开。实际活动通过 Resend 邮件通知，并在当次邮件或落地页说明优惠内容、有效期、使用次数以及是否与邀请奖励叠加。

Credit 不是现金余额，不可提现、转让或兑换现金。邀请奖励同样不可提现、转让或退款。

## 八、文件上传

V1 支持：

- PDF；
- TXT；
- Markdown；
- PNG；
- JPG；
- WebP。

文件存储不单独收费，解析、检索和进入模型上下文产生的成本计入 Credit。项目文件可以在同一项目的不同对话中复用，普通聊天文件不能自动进入全局记忆。

## 九、技术栈

- Next.js 16，App Router；
- React 19；
- TypeScript；
- Tailwind CSS 4；
- Drizzle ORM；
- Better Auth；
- next-intl；
- Vercel AI SDK；
- Creem 支付，同时保留 ShipAny 原有支付抽象；
- Vercel 部署；
- 当前生产数据库和存储配置以环境变量及 `docs/deployment-checklist.md` 为准。

项目由 ShipAny Template Two 改造而来。仓库中存在 ShipAny 的通用 AI、支付、订阅、生成器和示例页面，不代表 WebTools V1 都要使用。不要因为模板已有代码就默认保留某个产品能力。

## 十、关键目录

- `src/app/[locale]/(landing)/`：公开页面；
- `src/app/[locale]/(chat)/`：聊天工作区；
- `src/app/[locale]/(admin)/`：后台管理；
- `src/app/api/resource-assistant/`：当前站内资源助手接口；
- `src/app/api/chat/`：ShipAny 聊天接口；
- `src/shared/models/`：数据模型；
- `src/shared/blocks/`：业务界面区块；
- `src/config/locale/messages/`：中英文界面文案；
- `src/extensions/`：AI、支付、存储等 Provider；
- `docs/specs/webtools-ai-assistant-v1-spec.md`：AI 助手 V1 的权威需求文档；
- `docs/archive/content-platform-v1-spec.md`：已归档的内容平台规格；
- `docs/deployment-checklist.md`：部署环境检查；
- `docs/skills/product-idea-diagnosis/`：首个 Skill 草案。

根目录的 `index.html`、`博客.html`、`内容配置.js`、`博客索引.js` 和 `说明.md` 属于最初个人网站的遗留内容。除非任务明确涉及旧版个人网站，否则优先修改 Next.js 应用，不要误改这些遗留文件。

## 十一、开发约束

- 修改现有文件优先，不为假想需求提前扩展；
- 不顺手重构，不修改任务无关内容；
- 不覆盖或清理用户已有的未跟踪文件；
- 不提交密钥、Token、数据库地址或其他敏感信息；
- 环境变量只写变量名和示例占位符；
- 中英文公开页面需要同步考虑，不要只改一种语言；
- 中文注释只解释不明显的设计原因，默认不写注释；
- 用户可见模型与 Provider 必须保持解耦；
- 涉及 Credit 的操作必须有幂等键、预扣记录、实际结算和退款记录；
- 涉及用户数据的接口必须验证登录用户和资源所有权；
- 项目记忆必须保留来源消息，以便删除和重算；
- 不把用户私有聊天、文件或记忆写入公共知识库。

## 十二、验证命令

使用 pnpm，不混用 npm 或 yarn。

```bash
pnpm lint
pnpm build
pnpm format:check
```

根据改动范围补充运行相关测试或本地流程。涉及数据库结构时，先生成并审查迁移，不直接修改生产数据库。涉及页面时，应检查中文、英文、未登录、已登录和余额不足状态。

## 十三、文档优先级

发生冲突时按以下顺序处理：

1. 用户在当前任务中的明确要求；
2. 根目录 `AGENTS.md`；
3. `docs/specs/webtools-ai-assistant-v1-spec.md`；
4. 现有代码行为；
5. ShipAny 模板文档和遗留示例。

如果代码与 SPEC 不一致，不要默认代码正确。先指出差异，再根据当前任务决定修改代码还是更新文档。
